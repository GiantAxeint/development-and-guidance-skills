# 区块交接文档：`<区块ID>-<区块名>`

> 用途：多 agent 协作时，本区块的 agent 完成后填写，发给下一个 agent / 存入 `docs/handoff/`。
> 下一个 agent **必须先读本文档**再动手，不依赖对话记忆。

## 1. 基本信息

- 区块 ID / 名称：
- 执行 agent：
- 完成时间：
- 状态：☐ 完成  ☐ 部分完成（见"未决问题"）  ☐ 阻塞

## 2. 技术栈与版本（实测值，非记忆值）

- 语言/框架及版本：
- 关键依赖及版本（用 `npm ls` / `pip show` 实测回读）：
- 运行环境要求：

## 3. 本次改动内容

- 新增/修改的文件清单（路径 + 一句话职责）：
- 公共接口定义（函数签名 / 数据结构 / API 端点 / 端口），其他区块依赖这些接口：
- 数据流说明（谁调用谁、数据怎么流转）：

## 4. 如何运行与验证

- 启动命令：
- 验证步骤 / 自测结果：
- 需要其他区块配合的联调点：

## 5. 踩过的坑与未决问题

- 坑 1（现象 → 原因 → 解法）：
- 未决问题 / 待办（明确写给下一个 agent）：
- 给下一个 agent 的特别提醒（最容易踩的雷）：

## 6. git 记录

- 本区块 commit hash：`…`（保证可回滚到此版本）
- 分支说明：
